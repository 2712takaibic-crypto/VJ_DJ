# 📊 VJDJ プロジェクト進捗レポート

**更新日時**: 2026-09-05
**現在の状態**: plan.txt の要求機能はすべて実装済み
**成果物**: `output/hikari-mv.mp4` (1920×1080 / 30fps / 90.47秒 / H.264 + AAC / 126MB)

---

## 📈 ゴールの達成状況

| ゴール | 状態 |
|---|---|
| MV の制作 | ✅ 達成 |
| ツールの完成 (plan.txt の要求機能) | ✅ 達成 |

### plan.txt の要求と対応

| 要求 | 実装 |
|---|---|
| DJ用機能一式 (サンプリング、ミキシング) | 2デッキ / 3バンドEQ / クロスフェーダー / 8パッドサンプラー |
| DAW機能 (打ち込み、シーケンサー) | 16ステップ × 6トラック / 内蔵シンセ5音色 |
| VJ機能 (グリーンバック切り抜き、背景合成) | CbCr距離クロマキー + チョーク + デスピル |
| 各種フォーマットの取り込みと出力 | 任意の音声/映像を取り込み、mp4 で書き出し |
| Three.js 3D空間でのバーチャルライブMV | 実装・レンダリング済み |
| UnrealBloom 相当のポストプロセス | TSL版 bloom |
| fx-editor 素材のレーザー・電撃・パーティクル | 全実装。手続き的テクスチャも移植 |
| 舞台演出 | LEDウォール / トラス / 床グリッド / 照明 / 爆発・火花 |
| MCP による AI 操作 | 16ツール。`capture` で画を見ながら調整 |

---

## 🎛 ツールの構成

### ウィンドウ

- **Control** … 編集画面。プレビュー + 全パラメータ操作
- **Output** … 出力。2画面目があれば自動でそちらへ。カメラ操作とギズモもここ

### Control のパネル

| セクション | 内容 |
|---|---|
| 素材ライブラリ | 取り込み / 一覧 / 差し替え |
| DJ | DECK A/B、EQ、クロスフェーダー、レベルメーター、サンプラー |
| シーケンサー (DAW) | 16ステップグリッド、テンポ、ミュート |
| 配置の編集 | エフェクト位置、カメラモード、ギズモ切替 |
| クロマキー | inner / outer / despill / choke / brightness / マット表示 |
| 被写体 | 高さ / 左右 / 奥行き |
| 発光 (bloom) | strength / threshold / 音への追従 |
| LEDウォール | 色相 / 振れ幅 / 明るさ |
| エフェクト | レーザー / 電撃 / 爆発火花 / パーティクル |
| カメラ | 自動巡回 / ショット固定 / 手動 |

---

## 🧭 設計の柱

- **すべてが時刻の純関数**。リアルタイム再生と書き出しが同じコードを通るため、
  プレビューで見た画がそのまま書き出される。内部に累積状態を持たせない
- **乱数を使わない**。エフェクトの配置は拍のハッシュ、パーティクルはシード付き擬似乱数。
  `Math.random` は一箇所も使っていない
- **操作面は ShowParams ただ 1 つ**。UI・MCP・ギズモがすべて同じ値を触る
- **映像ソースは静止画列**。`HTMLVideoElement` の `currentTime` は
  フレーム精度が保証されず、決定性を満たせない
- **楽曲解析はオフラインで固定**。リアルタイム解析では書き出しのたびに結果が変わる
- **音声イベントは先読みスケジューリング**。JS のタイマー精度に依存させない

---

## 📏 実測値

| 項目 | 値 |
|---|---|
| 書き出し速度 | 90.47秒の映像を約 88秒 (実時間より速い) |
| プレビュー描画 | 1920×1080 で 60fps |
| 楽曲解析 | hikari.m4a → 130.009 BPM / 196拍 |
| 起動安定性 (S001-R) | 100回連続起動 100/100 成功 |
| RealtimeChannel RTT | p50 0.50ms / p99 12.90ms |
| オーディオクロック | 60秒で乖離 0.103ms (1.7ppm) |
| テスト | 層1: 9件 366ms / 層2 GPU: 3件 / 層3 音声: 3件 |

---

## ⚠️ 既知の制約

### 素材由来 (画質に影響)

`素材/green_back.mp4` はストック素材で **透かしが入っており、被写体の脚が下端で切れている**。
足元は床の発光で隠す構図にしてある。解像度も 596×336 と低く、
被写体を大きくすると粗が出る。**差し替え可能な素材があれば画質は上がる。**

### 機能面

- **キーロック未実装** … デッキのテンポを変えるとピッチも変わる。
  ピッチを保つには signalsmith-stretch を AudioWorklet で回す必要がある
- **シーケンサーのパターンが保存されない** … 起動ごとに空になる
- **シーケンサーの音が書き出しに含まれない** … 書き出しの音声は元の音源をそのまま多重化している
- **MIDI コントローラ未対応**
- **書き出しが UI から実行できない** … 現状は環境変数のみ
- **プロジェクトの保存/読込がない** … パラメータは起動ごとに既定値へ戻る

---

## 📦 再実行できるもの

| コマンド | 内容 |
|---|---|
| `npm start` | アプリ起動 |
| `npm run verify` | lint + typecheck + 層1テスト |
| `npm run test:all` | 層1〜3すべて |
| `npm run test:handshake` | 起動安定性 100回 |
| `npm run poc:platform / preview / audio` | PoC の再測定 |
| `VJDJ_IMPORT="a.mp4,b.mp3" npm start` | CLI から素材取り込み |
| `VJDJ_EXPORT=out.mp4 VJDJ_EXPORT_AUDIO=素材/hikari.m4a npm start` | MV 書き出し |
| `VJDJ_EXPORT_START=40 VJDJ_EXPORT_DURATION=0.5 …` | 画作り確認用の部分書き出し |

---

## 📚 文書

| ファイル | 内容 |
|---|---|
| `README.md` | 使い方 |
| `plan.txt` | 元の要望と進捗 |
| `.tmp/requirements.md` | 要件定義 |
| `.tmp/design.md` | 詳細設計。§10.5 に確定挙動 D-1〜D-8 |
| `.tmp/test_design.md` | テスト設計 |
| `.tmp/tasks.md` | タスク分解 (当初計画。方針転換後は plan.txt が正) |
| `.tmp/poc-results.md` | PoC 実測記録。設計判断の根拠 |
| `.tmp/perf-baseline.md` | 性能基準線 |

---

## 🔜 次にやるなら

優先度順の私見。

1. **プロジェクトの保存/読込** … 詰めた設定が起動ごとに消えるのは制作ツールとして痛い
2. **書き出しボタンを UI へ** … 環境変数でしか書き出せないのは使いにくい
3. **シーケンサーの音を書き出しへ** … DAW で作った音が MV に載らない
4. **キーロック** … DJ として繋ぐなら必要
5. **素材の差し替え** … 透かしのない素材があれば画質が大きく上がる
